# Инструкция по развертыванию на ngrok

Это руководство поможет вам развернуть веб-приложение "Справочник телефонов ОмГТУ" с использованием ngrok для доступа из интернета.

## Что такое ngrok?

ngrok - это инструмент, который создает безопасный туннель к вашему локальному серверу, позволяя получать к нему доступ из интернета через публичный URL.

## Предварительные требования

1. **XAMPP** (или другой локальный веб-сервер) должен быть установлен и запущен
2. **ngrok** должен быть установлен на вашем компьютере
3. Проект должен быть размещен в папке `C:\xampp\htdocs\oxirgi`

## Шаг 1: Установка ngrok

### Вариант 1: Скачать с официального сайта
1. Перейдите на https://ngrok.com/download
2. Скачайте версию для Windows
3. Распакуйте архив в удобное место (например, `C:\ngrok`)
4. Добавьте путь к ngrok в переменную окружения PATH (опционально)

### Вариант 2: Установка через Chocolatey (если установлен)
```powershell
choco install ngrok
```

## Шаг 2: Регистрация на ngrok (рекомендуется)

1. Зарегистрируйтесь на https://dashboard.ngrok.com/signup (бесплатно)
2. Получите ваш authtoken в разделе "Your Authtoken"
3. Выполните команду для авторизации:
```powershell
ngrok config add-authtoken YOUR_AUTHTOKEN
```

**Примечание:** Регистрация не обязательна, но дает:
- Стабильные URL (не меняются при каждом запуске)
- Больше возможностей настройки
- Лучшую безопасность

## Шаг 3: Запуск локального сервера

1. Откройте XAMPP Control Panel
2. Запустите Apache и MySQL
3. Убедитесь, что ваш проект доступен по адресу `http://localhost/oxirgi`

## Шаг 4: Запуск ngrok

### Базовый запуск (без регистрации)
Откройте PowerShell или Command Prompt и выполните:

```powershell
ngrok http 80
```

Или если Apache работает на другом порту (например, 8080):

```powershell
ngrok http 8080
```

### Запуск с указанием хоста (рекомендуется)
Если ваш проект находится в подпапке:

```powershell
ngrok http 80 --host-header="localhost/oxirgi"
```

### Запуск с фиксированным доменом (требует регистрации)
Если у вас есть зарегистрированный аккаунт и домен:

```powershell
ngrok http 80 --domain=your-domain.ngrok-free.app
```

## Шаг 5: Получение публичного URL

После запуска ngrok вы увидите что-то вроде:

```
Forwarding   https://abc123.ngrok-free.app -> http://localhost:80
```

Скопируйте URL `https://abc123.ngrok-free.app` - это ваш публичный адрес.

## Шаг 6: Настройка приложения для работы с ngrok

### Обновление базового URL (если необходимо)

Если в вашем приложении есть абсолютные пути, которые нужно обновить, проверьте файлы:
- `config/database.php` (если есть настройки URL)
- `.htaccess` (если используется)

### Настройка CORS (если нужно)

Если вы планируете использовать API, добавьте в начало ваших PHP файлов:

```php
header('Access-Control-Allow-Origin: *');
header('Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS');
header('Access-Control-Allow-Headers: Content-Type, Authorization');
```

## Шаг 7: Тестирование

1. Откройте публичный URL ngrok в браузере
2. Проверьте все основные функции:
   - Главная страница
   - Просмотр контактов
   - Поиск
   - Админ-панель (если доступна)

## Важные замечания

### Безопасность

⚠️ **ВНИМАНИЕ:** Публичный URL ngrok доступен всем в интернете!

1. **Не используйте ngrok для продакшн-окружения** с реальными данными
2. **Ограничьте доступ к админ-панели** - используйте сильные пароли
3. **Не храните чувствительные данные** в базе данных при использовании ngrok
4. **Используйте HTTPS** (ngrok предоставляет его автоматически)

### Ограничения бесплатного плана

- URL меняется при каждом перезапуске (если не зарегистрированы)
- Ограничение на количество подключений
- Ограничение на пропускную способность
- Может быть предупреждение ngrok при первом посещении

### Решение проблем

#### Проблема: "ngrok: command not found"
**Решение:** Добавьте ngrok в PATH или используйте полный путь к исполняемому файлу

#### Проблема: Страница не загружается
**Решение:** 
- Убедитесь, что Apache запущен
- Проверьте, что проект доступен локально по `http://localhost/oxirgi`
- Используйте флаг `--host-header` при запуске ngrok

#### Проблема: Статические файлы (CSS/JS) не загружаются
**Решение:** Проверьте пути к файлам в HTML - они должны быть относительными

#### Проблема: База данных не работает
**Решение:** 
- Убедитесь, что MySQL запущен в XAMPP
- Проверьте настройки подключения в `config/database.php`
- Убедитесь, что база данных создана и настроена

## Автоматизация запуска

### Создание bat-файла для Windows

Создайте файл `start-ngrok.bat`:

```batch
@echo off
echo Запуск ngrok...
start ngrok http 80 --host-header="localhost/oxirgi"
echo ngrok запущен! Проверьте окно ngrok для получения URL.
pause
```

### Создание PowerShell скрипта

Создайте файл `start-ngrok.ps1`:

```powershell
Write-Host "Запуск ngrok..." -ForegroundColor Green
Start-Process ngrok -ArgumentList "http 80 --host-header=`"localhost/oxirgi`""
Write-Host "ngrok запущен! Проверьте окно ngrok для получения URL." -ForegroundColor Green
```

## Альтернативные решения

Если ngrok не подходит, рассмотрите альтернативы:

1. **localtunnel** - бесплатный аналог ngrok
   ```powershell
   npm install -g localtunnel
   lt --port 80 --subdomain your-subdomain
   ```

2. **serveo** - SSH-туннель
   ```powershell
   ssh -R 80:localhost:80 serveo.net
   ```

3. **cloudflared** - от Cloudflare
   ```powershell
   cloudflared tunnel --url http://localhost:80
   ```

## Полезные ссылки

- [Официальный сайт ngrok](https://ngrok.com/)
- [Документация ngrok](https://ngrok.com/docs)
- [Dashboard ngrok](https://dashboard.ngrok.com/)

## Поддержка

Если у вас возникли проблемы:
1. Проверьте логи ngrok в окне терминала
2. Проверьте логи Apache в XAMPP
3. Убедитесь, что все сервисы запущены
4. Проверьте настройки файрвола Windows

---

**Удачного развертывания!** 🚀
