# Инструкция по публикации проекта на GitHub

Это пошаговое руководство поможет вам опубликовать проект "Справочник телефонов ОмГТУ" на GitHub.

## Что такое GitHub?

GitHub - это платформа для хостинга и совместной работы над кодом с использованием системы контроля версий Git. Это позволяет:
- Хранить код в облаке
- Отслеживать изменения
- Совместно работать над проектом
- Показывать свои проекты другим

## Предварительные требования

1. **Git** должен быть установлен на вашем компьютере
   - Скачать: https://git-scm.com/downloads
   - Проверить установку: `git --version` в командной строке

2. **Аккаунт на GitHub**
   - Зарегистрируйтесь на https://github.com (бесплатно)

## Шаг 1: Подготовка проекта

### 1.1. Создайте файл .gitignore

Создайте файл `.gitignore` в корне проекта со следующим содержимым:

```
# Конфигурационные файлы с секретами
config/database.php
config/yandex_oauth.php
.env

# Зависимости Composer
/vendor/

# Системные файлы
.DS_Store
Thumbs.db
*.log
*.tmp

# IDE файлы
.idea/
.vscode/
*.swp
*.swo

# Временные файлы
*.cache
```

**Важно:** Не публикуйте файлы с паролями и секретными ключами!

### 1.2. Создайте файл README.md (если еще нет)

Создайте основной README.md файл с описанием проекта:

```markdown
# Справочник телефонов ОмГТУ

Веб-приложение для управления контактной информацией сотрудников Омского государственного технического университета.

## Возможности

- Просмотр контактов сотрудников
- Поиск по имени, должности, отделу
- Управление подразделениями
- Админ-панель для управления данными
- Авторизация через Yandex

## Технологии

- PHP 7.4+
- MySQL
- Bootstrap 5
- Composer

## Установка

[Инструкции по установке]

## Лицензия

[Укажите лицензию]
```

## Шаг 2: Инициализация Git репозитория

### 2.1. Откройте терминал/командную строку

Перейдите в папку вашего проекта:

```bash
cd C:\xampp\htdocs\oxirgi
```

### 2.2. Инициализируйте Git репозиторий

```bash
git init
```

### 2.3. Добавьте все файлы

```bash
git add .
```

### 2.4. Сделайте первый коммит

```bash
git commit -m "Initial commit: Справочник телефонов ОмГТУ"
```

## Шаг 3: Создание репозитория на GitHub

### 3.1. Войдите в GitHub

Откройте https://github.com и войдите в свой аккаунт

### 3.2. Создайте новый репозиторий

1. Нажмите на "+" в правом верхнем углу
2. Выберите "New repository"
3. Заполните форму:
   - **Repository name:** `omgtu-phonebook` (или другое имя)
   - **Description:** "Справочник телефонов ОмГТУ"
   - **Visibility:** 
     - Public (публичный - все видят)
     - Private (приватный - только вы)
   - **НЕ** отмечайте "Initialize this repository with a README" (мы уже создали его)
4. Нажмите "Create repository"

### 3.3. Скопируйте URL репозитория

После создания вы увидите URL вида:
- `https://github.com/ваш-username/omgtu-phonebook.git`

## Шаг 4: Подключение локального репозитория к GitHub

### 4.1. Добавьте remote репозиторий

В терминале выполните (замените URL на ваш):

```bash
git remote add origin https://github.com/ваш-username/omgtu-phonebook.git
```

### 4.2. Переименуйте ветку в main (если нужно)

```bash
git branch -M main
```

### 4.3. Отправьте код на GitHub

```bash
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub (или токен доступа).

## Шаг 5: Настройка аутентификации

### 5.1. Создайте Personal Access Token (если требуется)

Если Git просит токен вместо пароля:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. Используйте токен вместо пароля при `git push`

## Шаг 6: Проверка

1. Обновите страницу репозитория на GitHub
2. Вы должны увидеть все ваши файлы
3. Поздравляем! Проект опубликован! 🎉

## Дальнейшая работа с Git

### Добавление изменений

```bash
# Посмотреть статус
git status

# Добавить измененные файлы
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

### Просмотр истории

```bash
git log
```

### Откат изменений

```bash
# Отменить изменения в файле (до коммита)
git checkout -- имя-файла.php

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1
```

## Полезные команды Git

```bash
# Клонировать репозиторий (для других разработчиков)
git clone https://github.com/username/repository.git

# Получить последние изменения
git pull

# Создать новую ветку
git checkout -b новая-ветка

# Переключиться на ветку
git checkout имя-ветки

# Слить ветки
git merge имя-ветки
```

## Что НЕ нужно публиковать

⚠️ **Никогда не публикуйте:**

- Файлы с паролями (`config/database.php`, `.env`)
- Секретные ключи API
- Личные данные пользователей
- Большие файлы (видео, изображения без оптимизации)
- Временные файлы

## Создание .gitignore (детальный пример)

Создайте файл `.gitignore` в корне проекта:

```
# Конфигурация
config/database.php
config/yandex_oauth.php
.env
.env.local

# Composer
/vendor/
composer.lock

# Системные файлы
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Логи
*.log
logs/
*.log.*

# Временные файлы
*.tmp
*.temp
*.cache
*.swp
*.swo
*~

# IDE
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# База данных (если есть дампы)
*.sql
*.dump

# Загрузки пользователей (если есть)
uploads/
files/
```

## Публикация через GitHub Desktop (альтернативный способ)

Если командная строка кажется сложной, используйте GitHub Desktop:

1. Скачайте: https://desktop.github.com/
2. Установите и войдите в аккаунт
3. File → Add local repository
4. Выберите папку проекта
5. Нажмите "Publish repository"
6. Заполните данные и опубликуйте

## Создание релизов (Releases)

Для создания версий проекта:

1. На GitHub перейдите в раздел "Releases"
2. Нажмите "Create a new release"
3. Укажите версию (например, v1.0.0)
4. Добавьте описание изменений
5. Опубликуйте

## Советы

1. **Делайте частые коммиты** - это помогает отслеживать изменения
2. **Пишите понятные сообщения коммитов** - "Исправлена ошибка" лучше чем "fix"
3. **Используйте ветки** для экспериментов
4. **Читайте изменения перед коммитом** - `git diff`
5. **Регулярно делайте push** - не теряйте код

## Решение проблем

### Ошибка: "remote origin already exists"

```bash
git remote remove origin
git remote add origin https://github.com/username/repo.git
```

### Ошибка: "failed to push some refs"

```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Забыли добавить файл в .gitignore

```bash
# Удалить файл из индекса, но оставить локально
git rm --cached имя-файла.php
git commit -m "Remove файл from tracking"
```

## Полезные ссылки

- [Официальная документация Git](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Интерактивное обучение Git](https://learngitbranching.js.org/)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

## Контакты

Если возникли вопросы:
- **Telegram:** @Abdullah_Hanafiy
- **Email:** Tagneuver@gmail.com

---

**Удачи с публикацией проекта!** 🚀
